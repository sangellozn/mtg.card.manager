import { ImageDetectionResult } from './image-detection-result';

describe('ImageDetectionResult', () => {
  it('should create an instance', () => {
    expect(new ImageDetectionResult()).toBeTruthy();
  });
});
