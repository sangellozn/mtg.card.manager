import { CardImagerySetsUpdate } from './card-imagery-sets-update';

describe('CardImagerySetsUpdate', () => {
  it('should create an instance', () => {
    expect(new CardImagerySetsUpdate()).toBeTruthy();
  });
});
